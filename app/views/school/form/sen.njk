{% extends "_layouts/form.njk" %}

{% set title = __("school.sen.title") %}

{% block form %}
  {{ radios({
    fieldset: {
      legend: {
        classes: "nhsuk-fieldset__legend--l",
        html: appHeading({
          caption: school.name,
          title: title
        })
      }
    },
    items: getBooleanItems(),
    decorate: "school.sen"
  }) }}
{% endblock %}
